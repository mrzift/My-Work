public static boolean isPal(String str){
        String rev = "";
        for (int i = 0; i <= str.length() - 1; i++) {
            rev += str.charAt(str.length() - 1 - i);
        }
        int smth = str.compareTo(rev);
        return smth == 0;
